#include <stdio.h>
int main (){
    int scores[7];
    scores [0] = -27;
    scores [1] = 87;
    scores [2] = 34;

    for (int i=0; i<3; i++)
    printf ("%d ", scores [i]);
    return 0;
}